{
  "type": "object",
  "properties": {
    "title": {
      "type": "string",
      "minLength": 1,
      "maxLength": 2000
    },
    "behavior": {
      "type": "string",
      "enum": [
        "always_on",
        "while_pressed",
        "blink",
        "unsupported"
      ]
    },
    "explanation": {
      "type": "string",
      "minLength": 1,
      "maxLength": 2000
    },
    "components": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[A-Za-z][A-Za-z0-9_-]{0,39}$"
          },
          "type": {
            "type": "string",
            "enum": [
              "power_supply",
              "led",
              "resistor",
              "button",
              "arduino_uno"
            ]
          },
          "value": {
            "type": "string",
            "minLength": 1,
            "maxLength": 2000
          },
          "purpose": {
            "type": "string",
            "minLength": 1,
            "maxLength": 2000
          }
        },
        "required": [
          "id",
          "type",
          "value",
          "purpose"
        ],
        "additionalProperties": false
      },
      "minItems": 0,
      "maxItems": 8
    }
  },
  "required": [
    "title",
    "behavior",
    "explanation",
    "components"
  ],
  "additionalProperties": false
}
